{
  "name": "co-asyncee",
  "version": "0.0.3",
  "repository": "git://github.com/axelhzf/co-asyncee.git",
  "description": "EventEmitter using generators",
  "dependencies": {
    "co": "^3.0.6",
    "co-parallel": "^1.0.0"
  },
  "devDependencies": {
    "chai": "^1.9.1",
    "mocha": "^1.20.1"
  },
  "scripts": {
    "test": "node --harmony node_modules/.bin/_mocha test.js --timeout 50000 --reporter spec"
  }
}
